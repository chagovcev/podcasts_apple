import { Header } from '@components';

const meta = {
  title: 'components/Header',
  component: Header,
  argTypes: {},
  tags: ['autodocs'],
};

export default meta;

export const Playground = {
  args: {},
};
